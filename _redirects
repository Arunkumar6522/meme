# Netlify redirects file (backup for SPA routing)
# This ensures all routes are handled by React Router

/*    /index.html   200

# API redirects (if you add serverless functions later)
/api/*  /.netlify/functions/:splat  200

# Security redirects
/admin/*  /index.html  200  Role=admin
/auth/*   /index.html  200